count_num = 10
section = 3
nums = list(range(10))


for i in range(count_num - section):
    sums = sum()
    if sum >= max_num:
        max_num = sum
    elif sum <= min_num:
        min_num = sum
result = max_num - min_num
# print("#{} {}".format(test_case, result))
